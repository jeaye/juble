def say_hi()
{
  var p = Person("Joob", 22);
  p.set_name("Jeaye");
  say("Name: " + p.get_name());
  say("Age: " + p.get_age().to_string());
  say("Alive: " + p.is_alive.to_string());
  say("Hitting with chainsaw...");
  var alive = false;
  p.is_alive = alive;
  say("Alive: " + p.is_alive.to_string());

  var c = Car();
  c.drive();
}
